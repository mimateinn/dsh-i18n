/**
 * dsh-i18n — DSH Web UI 多語言插件
 * Host 侧入口：纯 client 插件，此入口仅因 bundle 机制需要（no-op）。
 */
export declare const name = "dsh-i18n";
export declare const inject: string[];
export declare function apply(ctx: unknown): void;
